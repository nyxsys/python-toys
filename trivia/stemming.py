"""
This will contain a hash set that will
pull strings down into a bare minimum format
(stripping special characters, 
putting them all lowercase, stripping basic 
articles [the|a|and|etc] and then compare it
against another word to see if they hash 
down to the same thing, linguistically)


"""